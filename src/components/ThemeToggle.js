import React from "react";

const ThemeToggle = () => {
   return (
      <aside className="theme-toggle">
         <button className="theme-toogle__button">Change Theme</button>
      </aside>
   );
};

export default ThemeToggle;
